# Application Configuration
NODE_ENV=development
PORT=3000
APP_NAME=walansi-kontonbile
APP_VERSION=1.0.0

# Database Configuration (Cloud-agnostic - works with MongoDB Atlas, AWS DocumentDB, Azure Cosmos DB, etc.)
MONGODB_URI=mongodb://localhost:27017/walansi
# For cloud providers, use connection strings like:
# MongoDB Atlas: mongodb+srv://user:pass@cluster.mongodb.net/walansi
# AWS DocumentDB: mongodb://user:pass@docdb-cluster.cluster-xxxxx.us-east-1.docdb.amazonaws.com:27017/walansi?tls=true&tlsCAFile=rds-combined-ca-bundle.pem
# Azure Cosmos DB: mongodb://user:pass@account.mongo.cosmos.azure.com:10255/walansi?ssl=true

# Redis Configuration (Cloud-agnostic - works with AWS ElastiCache, Azure Cache, Google Cloud Memorystore, etc.)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TLS=false
# For cloud providers, use connection details:
# AWS ElastiCache: REDIS_HOST=your-cluster.xxxxx.cache.amazonaws.com
# Azure Cache: REDIS_HOST=your-cache.redis.cache.windows.net, REDIS_TLS=true
# Google Cloud Memorystore: REDIS_HOST=your-instance-ip

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# External APIs - Google Fact Check
GOOGLE_FACT_CHECK_API_KEY=your-google-api-key
GOOGLE_FACT_CHECK_API_URL=https://factchecktools.googleapis.com/v1alpha1

# External APIs - WhatsApp Business
WHATSAPP_API_TOKEN=your-whatsapp-business-api-token
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id
WHATSAPP_VERIFY_TOKEN=your-webhook-verify-token

# External APIs - Telegram Bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_API_URL=https://api.telegram.org/bot

# External APIs - SMS Gateway (Cloud-agnostic - works with Twilio, AWS SNS, Azure Communication Services, etc.)
SMS_PROVIDER=twilio
SMS_PROVIDER_API_KEY=your-sms-api-key
SMS_PROVIDER_API_SECRET=your-sms-api-secret
SMS_PROVIDER_FROM_NUMBER=your-sms-from-number
# For AWS SNS: SMS_PROVIDER=aws-sns, SMS_PROVIDER_API_KEY=AWS_ACCESS_KEY_ID, SMS_PROVIDER_API_SECRET=AWS_SECRET_ACCESS_KEY
# For Azure: SMS_PROVIDER=azure, SMS_PROVIDER_API_KEY=connection-string

# External APIs - Translation Service (Cloud-agnostic - works with Google Translate, AWS Translate, Azure Translator, etc.)
TRANSLATION_PROVIDER=google
TRANSLATION_API_KEY=your-translation-api-key
# For AWS Translate: TRANSLATION_PROVIDER=aws, TRANSLATION_API_KEY=AWS_ACCESS_KEY_ID, TRANSLATION_API_SECRET=AWS_SECRET_ACCESS_KEY
# For Azure Translator: TRANSLATION_PROVIDER=azure, TRANSLATION_API_KEY=subscription-key

# External APIs - Language Detection (usually bundled with translation service)
LANGUAGE_DETECTION_PROVIDER=google

# External APIs - Speech-to-Text (Cloud-agnostic - works with Google Speech-to-Text, AWS Transcribe, Azure Speech, etc.)
SPEECH_TO_TEXT_PROVIDER=google
SPEECH_TO_TEXT_API_KEY=your-speech-api-key
# For AWS Transcribe: SPEECH_TO_TEXT_PROVIDER=aws
# For Azure Speech: SPEECH_TO_TEXT_PROVIDER=azure

# External APIs - AI/LLM Service (Cloud-agnostic - works with OpenAI, AWS Bedrock, Azure OpenAI, Google Gemini, etc.)
AI_PROVIDER=openai
AI_API_KEY=your-ai-api-key
AI_API_URL=https://api.openai.com/v1
AI_MODEL=gpt-4
# For AWS Bedrock: AI_PROVIDER=aws-bedrock, AI_API_KEY=AWS_ACCESS_KEY_ID, AI_API_SECRET=AWS_SECRET_ACCESS_KEY
# For Azure OpenAI: AI_PROVIDER=azure-openai, AI_API_URL=https://your-resource.openai.azure.com
# For Google Gemini: AI_PROVIDER=google-gemini

# External APIs - Media Generation (Cloud-agnostic)
MEDIA_GENERATION_PROVIDER=openai
MEDIA_GENERATION_API_KEY=your-media-api-key

# Storage Configuration (Cloud-agnostic - works with AWS S3, Azure Blob, Google Cloud Storage, etc.)
STORAGE_PROVIDER=local
STORAGE_BUCKET=walansi-media
# For AWS S3: STORAGE_PROVIDER=aws-s3, STORAGE_BUCKET=your-bucket-name, AWS_ACCESS_KEY_ID=..., AWS_SECRET_ACCESS_KEY=..., AWS_REGION=us-east-1
# For Azure Blob: STORAGE_PROVIDER=azure-blob, STORAGE_BUCKET=your-container, AZURE_STORAGE_CONNECTION_STRING=...
# For Google Cloud Storage: STORAGE_PROVIDER=gcs, STORAGE_BUCKET=your-bucket, GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Logging Configuration
LOG_LEVEL=info
LOG_DIR=./logs

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
CORS_ORIGIN=*

# Health Check
HEALTH_CHECK_ENABLED=true

